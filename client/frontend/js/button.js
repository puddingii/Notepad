const join = document.getElementById("join");
if (join)
    join.addEventListener("click", () => location.href = "/join");

const login = document.getElementById("login");
if (login)
    login.addEventListener("click", () => location.href = "/login");